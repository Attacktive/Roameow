name: 'Release'
on:
  push:
    tags:
      - '**'
jobs:
  build-dmg:
    name: 'Build DMG'
    runs-on: 'macos-latest'
    permissions:
      contents: 'write'
    steps:
      - name: 'Checkout'
        uses: 'actions/checkout@v6'
        with:
          fetch-depth: 0

      - name: 'Build App'
        run: |
          xcodebuild archive \
            -project Roameow.xcodeproj \
            -scheme Roameow \
            -archivePath ./build/Roameow.xcarchive \
            -destination "platform=macOS" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: 'Export App'
        run: |
          mkdir -p ./build/export
          cp -R ./build/Roameow.xcarchive/Products/Applications/Roameow.app ./build/export/

      - name: 'Create DMG'
        run: |
          brew install create-dmg
          create-dmg \
            --volname "Roameow" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --app-drop-link 450 185 \
            "./build/Roameow.dmg" \
            "./build/export/Roameow.app"

      - name: 'Create ZIP for Sparkle'
        run: |
          cd ./build/export
          zip -r --symlinks ../Roameow.zip Roameow.app

      - name: 'Sign ZIP with Sparkle EdDSA key'
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          curl -L -o /tmp/Sparkle.tar.xz https://github.com/sparkle-project/Sparkle/releases/download/2.9.0/Sparkle-2.9.0.tar.xz
          tar -xf /tmp/Sparkle.tar.xz -C /tmp
          echo "$SPARKLE_PRIVATE_KEY" | /tmp/bin/sign_update --ed-key-file - -p ./build/Roameow.zip > ./build/Roameow.zip.sig

      - name: 'Generate appcast.xml'
        run: |
          TAG=${GITHUB_REF_NAME}
          ZIP_URL="https://github.com/Attacktive/Roameow/releases/download/${TAG}/Roameow.zip"
          ZIP_SIZE=$(stat -f%z ./build/Roameow.zip)
          ED_SIGNATURE=$(cat ./build/Roameow.zip.sig)
          VERSION=${TAG#v}
          BUILD=$(grep CURRENT_PROJECT_VERSION Version.xcconfig | sed 's/.*= *//')

          cat > ./build/appcast.xml <<EOF
          <?xml version="1.0" encoding="utf-8"?>
          <rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
            <channel>
              <title>Roameow</title>
              <item>
                <title>${VERSION}</title>
                <sparkle:version>${BUILD}</sparkle:version>
                <sparkle:shortVersionString>${VERSION}</sparkle:shortVersionString>
                <sparkle:minimumSystemVersion>12.0</sparkle:minimumSystemVersion>
                <pubDate>$(date -u +"%a, %d %b %Y %H:%M:%S +0000")</pubDate>
                <enclosure
                  url="${ZIP_URL}"
                  sparkle:edSignature="${ED_SIGNATURE}"
                  length="${ZIP_SIZE}"
                  type="application/octet-stream"
                />
              </item>
            </channel>
          </rss>
          EOF

      - name: 'Upload DMG, ZIP, and appcast.xml to Release'
        uses: 'softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b'
        with:
          files: |
            ./build/Roameow.dmg
            ./build/Roameow.zip
            ./build/appcast.xml
          generate_release_notes: true
